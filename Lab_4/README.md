Розширена система реляційної бази даних у пам'яті на Python.

## Class Design Decisions
- **Ієрархія типів даних:** Використано базовий клас `DataType` для стандартизації валідації. Це дозволяє легко додавати нові типи (наприклад, `DateType`) без зміни логіки таблиць.